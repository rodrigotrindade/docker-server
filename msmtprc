# Config do msmtp para ambiente local: envia tudo para o Mailpit,
# sem autenticacao e sem TLS (Mailpit escuta SMTP em texto puro na 1025).
defaults
auth           off
tls            off

account        mailpit
host           mailpit
port           1025
from           dev@localhost.test

account default : mailpit
